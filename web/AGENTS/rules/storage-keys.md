# localStorage 键名

统一 `apanel:` 前缀。**新增键必须带这个前缀**，避免和同域下别的东西撞名。

现有键：

- `apanel:locale`
- `apanel:scheme`
- `apanel:theme-hue`
- `apanel:data-layout`
- `apanel:files-path`
- `apanel:reader-wrap`
- `apanel:reader-line-numbers`
