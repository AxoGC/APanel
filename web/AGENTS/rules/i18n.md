# i18n

字典和 Provider 都在 `src/lib/i18n.tsx`，组件里 `useI18n()` 取 `t`。

- 扁平点号 key：`nav.database`、`logs.attach.stdin`
- 插值用 `{param}`：`t('files.deleted', { name })`
- **en 和 zh 两份字典必须同步增删**，缺 key 会直接把 key 本身渲染到界面上
- 组件里不要写字面量中文/英文，一律走 `t()`
